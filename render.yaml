services:
  - type: web
    name: aihcas
    runtime: docker
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        fromReferent: true # Ensure this is set in Render dashboard
      - key: TESSERACT_PATH
        value: /usr/bin/tesseract
